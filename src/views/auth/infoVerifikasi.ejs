<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification</title>
    <link rel="stylesheet" href="/output.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@3.0.0/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body
    class="tw-bg-gray-100 tw-h-screen tw-flex tw-items-center tw-justify-center"
  >
    <div
      class="tw-bg-white tw-p-8 tw-rounded-lg tw-shadow-lg tw-w-full tw-max-w-md"
    >
      <h2 class="tw-text-2xl tw-font-semibold tw-text-gray-800 tw-text-center">
        Verify Your Email
      </h2>
      <p class="tw-text-gray-600 tw-mt-4 tw-text-center">
        We have sent a verification email to your registered email address.
        Please check your inbox and confirm your email.
      </p>
      <p class="tw-text-gray-600 tw-mt-2 tw-text-center">
        If you did not receive the email, click the button below to resend it.
      </p>

      <button
        id="resendBtn"
        class="tw-mt-6 tw-bg-blue-500 tw-text-white tw-font-semibold tw-py-2 tw-px-4 tw-rounded tw-w-full tw-transition tw-duration-200 hover:tw-bg-blue-600"
      >
        Resend Verification Email
      </button>

      <p
        id="confirmationMessage"
        class="tw-text-green-500 tw-text-center tw-mt-4 tw-hidden"
      >
        Verification email has been resent successfully.
      </p>
    </div>

    <script>
      document
        .getElementById("resendBtn")
        .addEventListener("click", function () {
          // Simulate resending email
          setTimeout(() => {
            document
              .getElementById("confirmationMessage")
              .classList.remove("tw-hidden");
          }, 1000); // 1 second delay to simulate email resend
        });
    </script>
  </body>
</html>
